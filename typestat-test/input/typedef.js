const a = 123;
    /**
     * @exports
     * @typedef {string} Path just a path as a string
     */

/**
 * @typedef {{
 *   first: string;
 *   last: string;
 * }} Name a person's full name.
 * This is a lengthy description
 */
