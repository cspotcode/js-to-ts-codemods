/**
 * @template A,B
 * @template {number} C
 * @template D keep this comment
 * @param {A} foo Keep this comment
 * @param {B} bar
 * @param {D} biff
 * @param {C} baz
 * @return {string} name
 */
function jsdocGenerics(bar, foo, baz, biff) {}

/** @return {number} */
function foo() {}
/** @returns {number} */
function foo() {}
